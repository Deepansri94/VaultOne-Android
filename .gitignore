# Test & build reports
reports/
QA/selenium/reports/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Node / Electron build output
node_modules/
dist/
release/

# Android build output
app/build/
.gradle/
local.properties

# Release keystore — never commit the .jks, .b64 is safe (no private key exposure beyond repo access)
vaultone-release.jks
keystore_b64.txt

# OS
.DS_Store
Thumbs.db
